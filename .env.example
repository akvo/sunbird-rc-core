# =============================================================================
# Sunbird RC Environment Configuration
# =============================================================================
# Copy this file to .env and update values as needed

# -----------------------------------------------------------------------------
# Registry Service
# -----------------------------------------------------------------------------
RELEASE_VERSION=v2.0.2
HARD_DELETE_ENABLED=true

# W3C Verifiable Credentials - Signature & Certificate Configuration
# Set to true to enable cryptographic signing of credentials
SIGNATURE_ENABLED=true
CERTIFICATE_ENABLED=true

# Signature Provider Options:
# - dev.sunbirdrc.registry.service.impl.SignatureV1ServiceImpl (simpler, no DID service required)
# - dev.sunbirdrc.registry.service.impl.SignatureV2ServiceImpl (requires identity/DID service)
SIGNATURE_PROVIDER=dev.sunbirdrc.registry.service.impl.SignatureV1ServiceImpl

# -----------------------------------------------------------------------------
# Keycloak Authentication
# -----------------------------------------------------------------------------
KEYCLOAK_SECRET=your-keycloak-secret-here

# -----------------------------------------------------------------------------
# Vault Service (for secure key storage)
# -----------------------------------------------------------------------------
VAULT_ADDR=http://0.0.0.0:8200
VAULT_API_ADDR=http://0.0.0.0:8200
VAULT_ADDRESS=http://0.0.0.0:8200

# -----------------------------------------------------------------------------
# Identity Service (required for SignatureV2ServiceImpl)
# -----------------------------------------------------------------------------
VAULT_TOKEN=your-vault-token-here
VAULT_BASE_URL=http://vault:8200/v1
VAULT_ROOT_PATH=http://vault:8200/v1/kv
VAULT_TIMEOUT=5000
VAULT_PROXY=false
SIGNING_ALGORITHM=Ed25519Signature2020
JWKS_URI=
ENABLE_AUTH=false
WEB_DID_BASE_URL=https://example.com/identifier

# -----------------------------------------------------------------------------
# Credential Schema Service
# -----------------------------------------------------------------------------
IDENTITY_BASE_URL=http://identity:3332
# JWKS_URI=
# ENABLE_AUTH=false

# -----------------------------------------------------------------------------
# Credential Service
# -----------------------------------------------------------------------------
# IDENTITY_BASE_URL=http://identity:3332
SCHEMA_BASE_URL=http://credential-schema:3333
CREDENTIAL_SERVICE_BASE_URL=https://example.com/credentials
# JWKS_URI=
# ENABLE_AUTH=false

# -----------------------------------------------------------------------------
# Certificate API Service (for QR code generation)
# -----------------------------------------------------------------------------
# QR_TYPE options: W3C-VC (default), URL, URL-W3C-VC
# - W3C-VC: QR contains compressed credential JSON
# - URL: QR contains verification URL only
# - URL-W3C-VC: QR contains URL with embedded compressed data
# QR_TYPE=W3C-VC
# CERTIFICATE_DOMAIN_URL=https://your-domain.com
